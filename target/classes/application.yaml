spring:
  data:
    redis:
      host: 192.168.100.100
      port: 6379
      password: 123456
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: 1000ms
  profiles:
    group:
      #dev 如果只写dev的话 最右边优先级最高
      "dev": devServer,devSelf,devDB
    active: devServer